name: Lint Scripts

on:
  push:
    branches: [ main ]
    paths:
      - 'scripts/**'
      - '.github/scripts/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'scripts/**'
      - '.github/scripts/**'

jobs:
  linting:
    name: Linting
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Python 3.8
      uses: actions/setup-python@v3
      with:
        python-version: 3.8

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8

    - name: Lint with flake8
      run: |
        flake8 scripts/ .github/scripts/
